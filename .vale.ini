# Vale configuration for Microsoft Writing Style Guide enforcement

StylesPath = .github/styles

MinAlertLevel = suggestion

[*.md]
# Enable custom Blog styles only (Microsoft package requires vale sync)
BasedOnStyles = Blog

# Ignore code blocks
BlockIgnores = (?s) *```.*?```
# Ignore inline code
TokenIgnores = (`[^`]+`)
